# avoid wrongly picking up multiple versions
libarrow:
  - "*"
libarrow_all:
  - "*"

channel_sources:
  - conda-forge/label/orc_dev,conda-forge

# on osx, keep this in sync with llvm_version in meta.yaml.
c_compiler_version:            # [osx]
  - 18                         # [osx]
cxx_compiler_version:          # [osx]
  - 18                         # [osx]
c_compiler:            # [win]
  - vs2022             # [win]
cxx_compiler:          # [win]
  - vs2022             # [win]
